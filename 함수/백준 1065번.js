let input = require("fs").readFileSync("example.txt").toString().split(" "); //백준제출시'/dev/stdin'

let ip = parseInt(input[0]);

let num = [];

let a, b, c;
let n = 0;
a = parseInt(n / 100);
b = paresInt(n / 10);
c = parseInt(n % 10);

if (a > b) {
  a - b;
}
